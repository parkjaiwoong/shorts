version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd marketing && npm ci
    build:
      commands:
        - cd marketing && npm run build
  artifacts:
    baseDirectory: marketing/.next
    files:
      - '**/*'
  cache:
    paths:
      - marketing/node_modules/**/*
      - marketing/.next/cache/**/*
